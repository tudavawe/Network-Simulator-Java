package Entities;
import NetworkHelper.Address;
public class Device {

    private Address address;
    private int deviceNumber;

    // We also need some sort of router interface
    // Some sort of address
    // Maybe create address class?
}
